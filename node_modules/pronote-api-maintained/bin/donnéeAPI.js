
const { fork } = require('child_process');
const { join } = require('path');

require("../../../script.js");

DEMO_URL = "https://0220058u.pronote.toutatice.fr/pronote/";
DEMO_USERNAME = "h.cossec1";
DEMO_PASSWORD = "Maison99100";


 function test(type)
{
    fork(join(__dirname, 'fetch.js'), [DEMO_URL, DEMO_USERNAME, DEMO_PASSWORD, 'toutatice', type], { stdio: 'inherit' });
  
}

['student', 'parent'].forEach(test);
